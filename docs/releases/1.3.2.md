---
title: "Bohrin 1.3.2"
version: "1.3.2"
date: "2026-09-15"
tag: "v1.3.2"
breaking: false
summary: >
  Four engine fixes found by running Bohrin across 57 public environments: it no longer hangs
  after its report, it can now measure graders that require `Final Answer: X`, and two error
  messages now state the real cause instead of a wrong one.
---

## TL;DR

We ran Bohrin 1.3.1 across 57 public reinforcement-learning environments and read every
failure back to its cause. Four were in Bohrin. On some environments the `bohrin` command
printed its result and then never exited, which hangs a CI job. Graders that only accept an
answer written as `Final Answer: X` could never be measured. And two error messages named a
cause that wasn't true. All four are fixed. Nothing to change on upgrade.

## Upgrade impact

- **Breaking changes:** none
- **Action required:** none — `pip install --upgrade 'bohrin[verifiers]'`
- **New minimums:** none

## Highlights

- **`bohrin` exits when it is done** (#69). A hang after the report, inherited from a library
  the environment loads, can no longer keep your terminal or CI job waiting.
- **Graders that require `Final Answer: X` can now be measured** (#70). On the public
  environment that exposed this, the grader turned out to pay full marks for
  `Final Answer: B is not the answer.` on all 10 sampled tasks.
- **Error messages name the real cause** (#71): a missing API key is called a missing API
  key, and "verifiers too new" explains the pre-release that usually causes it.

## Added

- **Bohrin now recognises `Final Answer: X`.** Some graders only accept an answer written as
  `Final Answer: B`, usually because the prompt asks for exactly that. Bohrin learns the
  format a grader wants by trying presentations of your declared answer, and this one was
  missing, so such a grader could never be measured. It is now tried, after every existing
  presentation. On the public environment that exposed the gap, it also revealed a weakness:
  the grader pays full marks for `Final Answer: B is not the answer.`

## Fixed

- **Clearer message when a grader needs a key.** If an environment's grader stops because an
  API key such as `PRIME_API_KEY` isn't set, Bohrin now says exactly that, instead of
  suggesting the grader needs a live rollout.
- **The "verifiers too new" message explains what actually happened.** Usually nothing in
  your environment pinned a newer `verifiers`: a requirement that mentions a pre-release (for
  example `>=0.1.11.dev0`) makes pip install the newest pre-release. The fix is the same as
  before, `pip install 'verifiers<0.3.2'`, and 0.3.1 still satisfies that requirement.

- **No more hang after the report.** On one public environment, Bohrin printed its load
  error and the process then sat for 11 minutes until it was stopped. The cause was not in
  Bohrin: after the environment's dataset build failed, a data library it uses (Apache
  Arrow) waited forever while the process was shutting down, and plain Python without Bohrin
  hung the same way. But the hung terminal was Bohrin's, so the `bohrin` command now ends
  its process as soon as its output is written. Reports and `--json` files are written
  first, and every exit code is unchanged.

## Verified

- `ruff check` · `ruff format --check` · `mypy --strict` · `pytest` — all clean on Python
  3.11, 3.12 and 3.13; **382 tests**, up from 362 at 1.3.1.
- **Every fix was checked on the real environment that exposed it**, with Bohrin installed from
  the fix branch into a fresh virtual environment:
  - **Exit hang:** the environment that hung for 11 minutes now prints its load error and exits
    with code 2 in the same second.
  - **`Final Answer: X`:** the environment that read as *not measured* on all 10 sampled tasks
    is now measured — 10 of 10 accept a known-wrong answer (95% CI 72–100%), same cap and seed
    as the index run. Both findings were read against the grader's code: its parser takes the
    first A–F letter after the label, so `Final Answer: B is not the answer.` parses as `B`.
    A strict grader reading the same format is not accused (a test pins this).
  - **Credential message:** the environment whose grader needs `PRIME_API_KEY` now reports
    exactly that.
  - **"verifiers too new":** an environment resolving `verifiers` 0.3.2.dev95 now states the
    pre-release cause and that 0.3.1 still satisfies its requirement.
- **Root cause of the hang, measured rather than assumed:** plain Python with no Bohrin
  installed hung the same way; the only extra Python thread was a daemon; a native stack
  showed the C runtime's `exit` blocked in Apache Arrow's global thread-pool destructor.
- **Deliberately not changed:** the `verifiers` extra is not capped below 0.3.2 — Bohrin's v1
  adapter was checked on 0.3.2.dev95 and audits correctly, so a cap would lock v1 users out.

## Links

- Full changelog entry: [CHANGELOG.md](../../CHANGELOG.md)
- Compare: [`v1.3.1...v1.3.2`](https://github.com/prabhu-gopal/bohrin/compare/v1.3.1...v1.3.2)
- PRs in this release: #69, #70, #71
